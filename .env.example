# Claude Provider Configuration
# Choose between "anthropic" (default) or "vertex" for Google Vertex AI
CLAUDE_PROVIDER=anthropic

# Anthropic API Configuration (when CLAUDE_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_api_key_here

# Vertex AI Configuration (when CLAUDE_PROVIDER=vertex)
# Uncomment and set these values to use Vertex AI
# VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_REGION=us-central1
#
# Note: Vertex AI also requires Google Application Default Credentials
# Run: gcloud auth application-default login
